# AGENTS.md — Discovery Repo

> Generated initially by the `governed-exploratory-development` plugin version `{{PLUGIN_VERSION}}` on `{{GENERATED_AT}}`. This repository owns this file after generation; do not expect automatic synchronization.

## Repository role

This Discovery Repo investigates its approved charter under a fixed Governance
revision. `.governance/` is a read-only submodule. Its purpose is learning, not
automatic production delivery.

## First actions

1. Read `.governed/discovery.yaml`, `.governed/governance.yaml`, and
   `.governed/reading-scope.yaml` before permitted Governance bodies.
2. Validate source, commit, scope, parent Git link, and the exact submodule
   checkout. Surface dirty or unexpected state without resetting it.
3. Restate the objective, question, success criteria, and non-goals before code.

## Reading boundary

The complete Governance tree may be present. Full permits bodies at the pinned
commit when Product Access Mode permits them. Curated permits only explicitly
allowed paths; excluded and unlisted bodies must not be read, searched,
summarized, retrieved indirectly, or delegated. Path names and minimal authority
metadata may be inspected for validation. These are workflow boundaries, not a
hard filesystem sandbox.

## Discipline

Apply Constitution > Policies > Specifications > Active ADRs. Do not edit or
advance `.governance/`; a changed revision requires a successor Repo. Creation
stages only `.gitmodules` and the Governance Git link, creates no commit, and
leaves other generated files unstaged. Record failed approaches and limitations.

## Completion

Use Discovery Review. Surface the durable Discovery Report before independent
promotion and retention choices. Keep Governance promotion, Code Promotion, and
repository retention independent.
