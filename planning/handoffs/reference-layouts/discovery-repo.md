# Discovery Repository Reference Layout

Initial state:

```text
<project>-disc-0042-short-slug/
├── .git/
├── AGENTS.md
├── DISCOVERY.yaml
└── .governance/
    ├── SNAPSHOT.yaml
    └── <selected immutable artifacts>
```

Everything else is generated by the experiment itself.

The repo must not inherit Product CI, lockfiles, dependency files, directory conventions, source, tests, or history unless the charter and selected Product-access mode explicitly justify them.
